[build]
builder = "NIXPACKS"

[[services]]
name = "api"
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[services.variables]
PORT = "8000"

[[services]]
name = "mcp"
startCommand = "python mcp_server.py"

[services.variables]
MCP_PORT = "8001"
