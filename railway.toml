[build]
builder = "NIXPACKS"

[[services]]
name = "api"
startCommand = "uvicorn api.api:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'"

[[services]]
name = "mcp"
startCommand = "python mcp_server.py"
